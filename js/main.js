//jshint esversion:6

const button = document.querrySelector('.navbar-toggler');
const shoecase = document.querrySelector('.showcase');

button.addEventListener('click', () => {
showcase.style.background = 'rgba(0,0,0,0.5)';
});
